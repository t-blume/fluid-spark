spark {
  name  = "SchemEX-Test"
  master = "local[1]"
  logDir = "/tmp/spark-events"
}

database {
  url = "remote:localhost"
  name = "till-test"
  username = "root"
  password = "rootpwd"
}

input {
  filename = ["resources/manual-test-0.nq", "resources/manual-test-1.nq"]
  graphLabel = "type"
  namespace = "http://informatik.uni-kiel.de/fluid#"
  defaultSource = "fluid"
}

schema {
  indexModel = "schemex"
}

igsi {
  clearRepo = true
  trackChanges = false
  minWait = 2000
}