spark {
  name  = "SchemEX-Test-gold"
  master = "local[*]"
  logDir = "/tmp/spark-events"
  memory = "1g"
  cores = "1"
  partitions = 1
}

database {
  url = "remote:localhost"
  name = "till-test-gold"
  username = "root"
  password = "rootpwd"
}

input {
  folder = "resources"
  filename = ["manual-test-3.nq"]
  graphLabel = "type"
  namespace = "http://informatik.uni-kiel.de/fluid#"
  defaultSource = "fluid"
}

schema {
  indexModel = "schemex"
}

igsi {
  clearRepo = true
  trackChanges = false
  alsoBatch = false
  minWait = 2000
}