spark {
  name  = "SchemEX-Test-gold"
  master = "local[1]"
  logDir = "/tmp/spark-events"
}

database {
  url = "remote:localhost"
  name = "till-test-gold"
  username = "root"
  password = "rootpwd"
}

input {
  filename = ["resources/manual-test-3.nq"]
  graphLabel = "type"
}

schema {
  indexModel = "schemex"
}

igsi {
  clearRepo = true
  trackChanges = false
  minWait = 2000
}