spark {
  name  = "SchemEX-ScaleTest"
  master = "local[10]"
  logDir = "/tmp/spark-events"
  memory = "1g"

}

database {
  url = "remote:localhost"
  name = "till-test"
  username = "root"
  password = "rootpwd"
}

input {
  filename = ["resources/manual-test-aggregate.nq"]
  graphLabel = "type"
  namespace = "http://informatik.uni-kiel.de/fluid#"
  defaultSource = "fluid"
}

schema {
  indexModel = "schemex"
}

igsi {
  clearRepo = true
  minWait = 2000
  trackChanges = true
  logChangesDir = "experiments/aggregate-test"
}